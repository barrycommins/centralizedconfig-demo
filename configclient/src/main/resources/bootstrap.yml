spring:
  application:
    name: config-client
  cloud:
    config:
      uri: http://localhost:8888
management:
  security:
    enabled: false
---
spring:
  cloud:
    config:
      uri: http://config-server:8888
  profiles:
    - docker